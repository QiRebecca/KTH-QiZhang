# Appendix

The smoke pipeline uses deterministic synthetic functions and lightweight CPU models to validate repository mechanics before GPU allocation.
The main run should use `configs/main_qwen25coder_1p5b_L18.yaml` and update the README with full Qwen2.5-Coder-1.5B metrics.
